version: '3.8'

services:
  web:
    build: .
    container_name: psychosassquatch-web
    ports:
      - "32781:80"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://127.0.0.1/"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
